name: Umsatzübersicht aktualisieren

on:
  schedule:
    - cron: '10 5 15,30 * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  refresh:
    name: SharePoint-Umsatzwerte einlesen
    runs-on: ubuntu-latest
    steps:
      - name: Umsatzübersicht serverseitig aktualisieren
        env:
          SCHEDULE_KEY: ${{ secrets.SETUP_KEY }}
        run: |
          set -euo pipefail
          test -n "$SCHEDULE_KEY"
          response_file="$(mktemp)"
          status="$(curl --silent --show-error --location \
            --output "$response_file" --write-out '%{http_code}' \
            --request POST \
            --header "X-SVNET-Schedule-Key: $SCHEDULE_KEY" \
            'https://www.sv-netzwerk.eu/intern/api/revenue-summary-v2.php?action=scheduled')"
          cat "$response_file"
          test "$status" = '200'
          python3 - "$response_file" <<'PY'
          import json, sys
          with open(sys.argv[1], encoding='utf-8') as source:
              payload = json.load(source)
          if payload.get('ok') is not True or payload.get('scheduled') is not True:
              raise SystemExit('Umsatzaktualisierung wurde nicht bestätigt.')
          PY

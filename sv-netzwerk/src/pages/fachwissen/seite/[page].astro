---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import LibraryFilters from '../../../components/LibraryFilters.astro';
import LibraryGrid from '../../../components/LibraryGrid.astro';
import Pagination from '../../../components/Pagination.astro';
import { PAGE_SIZE, sortedLibrary } from '../../../data/library';

export function getStaticPaths() {
  const totalPages = Math.ceil(sortedLibrary.length / PAGE_SIZE);
  return Array.from({ length: Math.max(0, totalPages - 1) }, (_, index) => {
    const page = index + 2;
    return { params: { page: String(page) }, props: { page, totalPages } };
  });
}

const { page, totalPages } = Astro.props;
const start = (page - 1) * PAGE_SIZE;
const pageItems = sortedLibrary.slice(start, start + PAGE_SIZE);
---
<BaseLayout
  title={`Fachartikel – Seite ${page} von ${totalPages} | SV-Netzwerk`}
  description={`Fachbeiträge des SV-Netzwerks zu Leitungswasser-, Brand-, Feuchte-, Gebäude- und Komplexschäden – Seite ${page} von ${totalPages}.`}
  breadcrumbs={[{ label: 'Start', href: '/' }, { label: 'Fachartikel', href: '/fachwissen/' }, { label: `Seite ${page}` }]}
>
  <section class="sv-section">
    <div class="sv-container">
      <h1>{`Fachartikel – Seite ${page} von ${totalPages}`}</h1>
      <LibraryFilters />
      <LibraryGrid items={pageItems} />
      <Pagination currentPage={page} totalPages={totalPages} />
    </div>
  </section>
</BaseLayout>

---
import { getCollection } from 'astro:content';
export async function getStaticPaths(){
  const entries=await getCollection('videos');
  return entries.map((entry)=>({params:{id:entry.id}}));
}
return Astro.redirect('/fachwissen/', 302);
---
